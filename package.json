{
  "name": "static_website",
  "version": "1.0.0",
  "description": "A boiler plate for a static website",
  "main": "app.js",
  "author": "Robert",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "gulp serve",
    "clean-build": "yarn clean && yarn build",
    "build": "set NODE_ENV=production&&gulp build",
    "clean": "rm -rf ./dist/**",
    "clean-node": "rm -rf ./node_modules"
  },
  "devDependencies": {
    "browser-sync": "^2.24.4",
    "gulp": "^3.9.1",
    "gulp-cli": "^2.0.1",
    "gulp-concat": "^2.6.1",
    "gulp-csso": "^3.0.1",
    "gulp-pug": "^4.0.1",
    "gulp-sass": "^4.0.1",
    "gulp-sourcemaps": "^2.6.4",
    "gulp-uglify": "^3.0.0",
    "node-sass": "^4.9.0",
    "pug": "^2.0.3"
  }
}
